# ast-tools（ast_grep + ast_edit）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 给 Pi 增加一个纯扩展 `ast-tools`，提供 `ast_grep`（结构化查询）与 `ast_edit`（结构化批量重写），底层用 ast-grep 官方 `@ast-grep/napi`。

**架构：** 独立扩展包 `extensions/ast-tools/`。纯逻辑（扩展名→Lang 映射、metavariable 模板展开）抽成可单测模块；`grep.ts`/`edit.ts` 用 `@ast-grep/napi` 真实解析做集成测试；`index.ts` 注册两个工具，napi 加载失败则不注册（fail-soft）。第一版直接应用编辑（`dryRun` 可选），不接 internal-urls / resolve（归波2）。

**技术栈：** TypeScript（ESM）、`@ast-grep/napi`、`tinyglobby`（glob 收集）、`typebox`（工具 schema）、`vitest`（测试）。

**规格依据：** `docs/superpowers/specs/2026-06-17-ast-tools-design.md`。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| 创建 `extensions/ast-tools/package.json` | 扩展包元数据 + 依赖（`@ast-grep/napi`、`tinyglobby`、`typebox`、`vitest`）|
| 创建 `extensions/ast-tools/lang.ts` | `extToLang(filename)` 扩展名→Lang 映射（纯）；`collectFiles(paths, cwd)` glob 收集 |
| 创建 `extensions/ast-tools/lang.test.ts` | 测 `extToLang` 与 `collectFiles` |
| 创建 `extensions/ast-tools/rewrite.ts` | `expandTemplate(out, resolver)` metavariable 展开（纯）|
| 创建 `extensions/ast-tools/rewrite.test.ts` | 测 `expandTemplate`（$VAR/$$$ARGS/$$/缺失）|
| 创建 `extensions/ast-tools/grep.ts` | `runAstGrep({pat, paths, skip, cwd})` 执行 + 格式化 |
| 创建 `extensions/ast-tools/grep.test.ts` | napi 集成测：命中/skip/空 |
| 创建 `extensions/ast-tools/edit.ts` | `runAstEdit({ops, paths, dryRun, cwd})` 执行（含 resolver 构造）|
| 创建 `extensions/ast-tools/edit.test.ts` | napi 集成测：应用/dryRun/多 op/无匹配 |
| 创建 `extensions/ast-tools/index.ts` | 注册 `ast_grep`/`ast_edit`，napi 缺失降级 |
| 修改 `extensions/index.ts` | 聚合导入 `astTools` 并加入 `allExtensions` |

---

## 任务 1：扩展脚手架 + 依赖

**文件：**
- 创建：`extensions/ast-tools/package.json`
- 创建：`extensions/ast-tools/index.ts`（临时空骨架）

- [ ] **步骤 1：写 package.json**

```json
{
  "name": "pi-ast-tools",
  "version": "0.1.0",
  "description": "Structural code search and rewrite for the Pi coding agent: ast_grep + ast_edit, backed by @ast-grep/napi.",
  "private": true,
  "type": "module",
  "keywords": ["pi-package", "pi-extension", "ast", "ast-grep", "codemod"],
  "license": "MIT",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "typebox": "*"
  },
  "dependencies": {
    "@ast-grep/napi": "^0.39.6",
    "tinyglobby": "^0.2.15"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*",
    "vitest": "^4.1.8"
  }
}
```

- [ ] **步骤 2：写临时空骨架 `index.ts`**

```ts
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function (_pi: ExtensionAPI) {
  console.error("[ast-tools] extension loaded");
}
```

- [ ] **步骤 3：安装依赖并核对 Lang enum 成员名**

运行：`cd extensions/ast-tools && npm install`
预期：装上 `@ast-grep/napi`、`tinyglobby`。

运行：`node -e "console.log(Object.keys(require('@ast-grep/napi').Lang))"`
预期：打印内置语言名（如 `JavaScript TypeScript Tsx Python Rust Go Java C Cpp ...`）。**记录实际成员名**，任务 2 的映射表以此为准。

- [ ] **步骤 4：Commit**

```bash
git add extensions/ast-tools/package.json extensions/ast-tools/index.ts
git commit -m "feat(ast-tools): scaffold extension package + deps"
```

---

## 任务 2：`extToLang` 扩展名映射（纯函数 TDD）

**文件：**
- 创建：`extensions/ast-tools/lang.ts`
- 测试：`extensions/ast-tools/lang.test.ts`

- [ ] **步骤 1：写失败的测试**

```ts
// lang.test.ts
import { describe, expect, it } from "vitest";
import { Lang } from "@ast-grep/napi";
import { extToLang } from "./lang.js";

describe("extToLang", () => {
  it("maps common extensions to Lang", () => {
    expect(extToLang("a.ts")).toBe(Lang.TypeScript);
    expect(extToLang("a.tsx")).toBe(Lang.Tsx);
    expect(extToLang("a.js")).toBe(Lang.JavaScript);
    expect(extToLang("a.mjs")).toBe(Lang.JavaScript);
    expect(extToLang("a.py")).toBe(Lang.Python);
    expect(extToLang("a.go")).toBe(Lang.Go);
    expect(extToLang("a.rs")).toBe(Lang.Rust);
  });
  it("is case-insensitive and handles paths", () => {
    expect(extToLang("/abs/Dir/File.TS")).toBe(Lang.TypeScript);
  });
  it("returns null for unknown extensions", () => {
    expect(extToLang("a.unknownext")).toBeNull();
    expect(extToLang("noext")).toBeNull();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd extensions/ast-tools && npx vitest run lang.test.ts`
预期：FAIL，报错 `extToLang is not a function` / 模块不存在。

- [ ] **步骤 3：写最少实现（lang.ts，先只 extToLang）**

```ts
// lang.ts
import { Lang } from "@ast-grep/napi";

// 扩展名（小写，不含点）→ Lang。以任务1步骤3核对到的实际 enum 成员名为准。
const EXT_TO_LANG: Record<string, Lang> = {
  js: Lang.JavaScript, jsx: Lang.JavaScript, mjs: Lang.JavaScript, cjs: Lang.JavaScript,
  ts: Lang.TypeScript, mts: Lang.TypeScript, cts: Lang.TypeScript,
  tsx: Lang.Tsx,
  py: Lang.Python, pyi: Lang.Python,
  go: Lang.Go,
  rs: Lang.Rust,
  java: Lang.Java,
  c: Lang.C, h: Lang.C,
  cc: Lang.Cpp, cpp: Lang.Cpp, cxx: Lang.Cpp, hpp: Lang.Cpp, hh: Lang.Cpp,
  cs: Lang.CSharp,
  rb: Lang.Ruby,
  php: Lang.Php,
  html: Lang.Html, css: Lang.Css, json: Lang.Json, yaml: Lang.Yaml, yml: Lang.Yaml,
};

export function extToLang(filename: string): Lang | null {
  const dot = filename.lastIndexOf(".");
  if (dot < 0) return null;
  const ext = filename.slice(dot + 1).toLowerCase();
  return EXT_TO_LANG[ext] ?? null;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd extensions/ast-tools && npx vitest run lang.test.ts`
预期：PASS（若某 Lang 成员名不符，按步骤1记录修正映射表）。

- [ ] **步骤 5：Commit**

```bash
git add extensions/ast-tools/lang.ts extensions/ast-tools/lang.test.ts
git commit -m "feat(ast-tools): extToLang extension mapping"
```

---

## 任务 3：`collectFiles` glob 收集（集成测试）

**文件：**
- 修改：`extensions/ast-tools/lang.ts`（追加 `collectFiles`）
- 测试：`extensions/ast-tools/lang.test.ts`（追加）

- [ ] **步骤 1：追加失败的测试**

```ts
// lang.test.ts 追加
import { mkdtempSync, mkdirSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { collectFiles } from "./lang.js";

describe("collectFiles", () => {
  it("collects supported files from dir, file, and glob", async () => {
    const root = mkdtempSync(join(tmpdir(), "ast-cf-"));
    mkdirSync(join(root, "sub"));
    writeFileSync(join(root, "a.ts"), "const a = 1");
    writeFileSync(join(root, "sub", "b.py"), "x = 1");
    writeFileSync(join(root, "note.txt"), "ignored");

    const byDir = await collectFiles(["."], root);
    const langs = byDir.map((f) => f.rel).sort();
    expect(langs).toEqual(["a.ts", "sub/b.py"]); // note.txt 被跳过（无映射）

    const byFile = await collectFiles(["a.ts"], root);
    expect(byFile).toHaveLength(1);
    expect(byFile[0].rel).toBe("a.ts");

    const byGlob = await collectFiles(["**/*.py"], root);
    expect(byGlob.map((f) => f.rel)).toEqual(["sub/b.py"]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd extensions/ast-tools && npx vitest run lang.test.ts`
预期：FAIL，`collectFiles is not a function`。

- [ ] **步骤 3：实现 `collectFiles`（追加到 lang.ts）**

```ts
// lang.ts 追加
import { glob } from "tinyglobby";
import { statSync } from "node:fs";
import { isAbsolute, relative, resolve } from "node:path";
import type { Lang } from "@ast-grep/napi";

export interface CollectedFile {
  abs: string;
  rel: string;
  lang: Lang;
}

// 把一个 path 规范成 tinyglobby pattern：目录→ dir/**/*，文件/glob 原样（POSIX 斜杠）。
function toPattern(p: string, cwd: string): string {
  const hasGlob = /[*?[\]{}]/.test(p);
  if (!hasGlob) {
    const abs = isAbsolute(p) ? p : resolve(cwd, p);
    try {
      if (statSync(abs).isDirectory()) return `${p.replace(/\\/g, "/").replace(/\/$/, "")}/**/*`;
    } catch {
      // 不存在就当普通模式交给 glob（返回空）
    }
  }
  return p.replace(/\\/g, "/");
}

export async function collectFiles(paths: string[], cwd: string): Promise<CollectedFile[]> {
  const patterns = paths.map((p) => toPattern(p, cwd));
  const hits = await glob(patterns, { cwd, absolute: true, onlyFiles: true, dot: false });
  const out: CollectedFile[] = [];
  const seen = new Set<string>();
  for (const abs of hits) {
    if (seen.has(abs)) continue;
    seen.add(abs);
    const lang = extToLang(abs);
    if (lang === null) continue;
    out.push({ abs, rel: relative(cwd, abs).replace(/\\/g, "/"), lang });
  }
  out.sort((a, b) => a.rel.localeCompare(b.rel));
  return out;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd extensions/ast-tools && npx vitest run lang.test.ts`
预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add extensions/ast-tools/lang.ts extensions/ast-tools/lang.test.ts
git commit -m "feat(ast-tools): collectFiles via tinyglobby (file/dir/glob)"
```

---

## 任务 4：`expandTemplate` metavariable 展开（纯函数 TDD，核心）

**文件：**
- 创建：`extensions/ast-tools/rewrite.ts`
- 测试：`extensions/ast-tools/rewrite.test.ts`

- [ ] **步骤 1：写失败的测试**

```ts
// rewrite.test.ts
import { describe, expect, it } from "vitest";
import { expandTemplate, type MetaResolver } from "./rewrite.js";

const resolver: MetaResolver = {
  single: (n) => ({ A: "x", B: "y" } as Record<string, string>)[n] ?? null,
  multi: (n) => (n === "ARGS" ? "1, 2, 3" : null),
};

describe("expandTemplate", () => {
  it("expands single metavariables", () => {
    expect(expandTemplate("logger.info($A)", resolver)).toBe("logger.info(x)");
    expect(expandTemplate("$A + $B", resolver)).toBe("x + y");
  });
  it("expands multi metavariable $$$NAME as raw slice", () => {
    expect(expandTemplate("f($$$ARGS)", resolver)).toBe("f(1, 2, 3)");
  });
  it("treats $$ as literal dollar", () => {
    expect(expandTemplate("price = $$5", resolver)).toBe("price = $5");
  });
  it("missing variable expands to empty string", () => {
    expect(expandTemplate("$Z!", resolver)).toBe("!");
  });
  it("longest-match: $$$ before $", () => {
    expect(expandTemplate("$$$ARGS|$A", resolver)).toBe("1, 2, 3|x");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd extensions/ast-tools && npx vitest run rewrite.test.ts`
预期：FAIL，模块不存在。

- [ ] **步骤 3：实现 `rewrite.ts`**

```ts
// rewrite.ts
export interface MetaResolver {
  /** $NAME 单匹配文本，无则 null */
  single(name: string): string | null;
  /** $$$NAME 多匹配的原始源码切片（含原分隔符），无则 null */
  multi(name: string): string | null;
}

// 顺序：先 $$$NAME（多），再 $NAME（单），再 $$（字面 $）。
const META_RE = /\$\$\$([A-Z_][A-Z0-9_]*)|\$([A-Z_][A-Z0-9_]*)|\$\$/g;

export function expandTemplate(out: string, r: MetaResolver): string {
  return out.replace(META_RE, (m, multi: string | undefined, single: string | undefined) => {
    if (m === "$$") return "$";
    if (multi !== undefined) return r.multi(multi) ?? "";
    if (single !== undefined) return r.single(single) ?? "";
    return m;
  });
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd extensions/ast-tools && npx vitest run rewrite.test.ts`
预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add extensions/ast-tools/rewrite.ts extensions/ast-tools/rewrite.test.ts
git commit -m "feat(ast-tools): expandTemplate metavariable expansion"
```

---

## 任务 5：`runAstGrep` + ast_grep 工具（napi 集成）

**文件：**
- 创建：`extensions/ast-tools/grep.ts`
- 测试：`extensions/ast-tools/grep.test.ts`

- [ ] **步骤 1：写失败的测试**

```ts
// grep.test.ts
import { describe, expect, it } from "vitest";
import { mkdtempSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { runAstGrep } from "./grep.js";

function fixture(): string {
  const root = mkdtempSync(join(tmpdir(), "ast-grep-"));
  writeFileSync(join(root, "a.ts"), "console.log(1)\nconsole.log(2)\nfoo(3)\n");
  return root;
}

describe("runAstGrep", () => {
  it("finds matches with location", async () => {
    const root = fixture();
    const res = await runAstGrep({ pat: "console.log($A)", paths: ["a.ts"], skip: 0, cwd: root });
    expect(res.totalMatches).toBe(2);
    expect(res.matches[0]).toMatchObject({ rel: "a.ts", line: 1 });
    expect(res.matches[1].line).toBe(2);
  });
  it("supports skip and reports empty", async () => {
    const root = fixture();
    const skipped = await runAstGrep({ pat: "console.log($A)", paths: ["a.ts"], skip: 1, cwd: root });
    expect(skipped.matches).toHaveLength(1);
    expect(skipped.matches[0].line).toBe(2);

    const none = await runAstGrep({ pat: "nope($A)", paths: ["a.ts"], skip: 0, cwd: root });
    expect(none.totalMatches).toBe(0);
    expect(none.matches).toHaveLength(0);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd extensions/ast-tools && npx vitest run grep.test.ts`
预期：FAIL，模块不存在。

- [ ] **步骤 3：实现 `grep.ts`**

```ts
// grep.ts
import { readFileSync } from "node:fs";
import { parse } from "@ast-grep/napi";
import { collectFiles } from "./lang.js";

export interface AstGrepMatch {
  rel: string;
  line: number; // 1-based
  column: number; // 1-based
  text: string;
}
export interface AstGrepResult {
  matches: AstGrepMatch[];
  totalMatches: number;
  filesSearched: number;
  parseErrors: string[];
}
export interface AstGrepArgs {
  pat: string;
  paths: string[];
  skip: number;
  cwd: string;
  limit?: number; // 默认 200
}

export async function runAstGrep(args: AstGrepArgs): Promise<AstGrepResult> {
  const limit = args.limit ?? 200;
  const files = await collectFiles(args.paths, args.cwd);
  const all: AstGrepMatch[] = [];
  const parseErrors: string[] = [];
  for (const f of files) {
    let src: string;
    try {
      src = readFileSync(f.abs, "utf8");
    } catch (err) {
      parseErrors.push(`${f.rel}: read failed ${(err as Error).message}`);
      continue;
    }
    try {
      const nodes = parse(f.lang, src).root().findAll(args.pat);
      for (const n of nodes) {
        const r = n.range();
        all.push({ rel: f.rel, line: r.start.line + 1, column: r.start.column + 1, text: n.text() });
      }
    } catch (err) {
      parseErrors.push(`${f.rel}: ${(err as Error).message}`);
    }
  }
  all.sort((a, b) => a.rel.localeCompare(b.rel) || a.line - b.line || a.column - b.column);
  const visible = all.slice(args.skip, args.skip + limit);
  return { matches: visible, totalMatches: all.length, filesSearched: files.length, parseErrors };
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd extensions/ast-tools && npx vitest run grep.test.ts`
预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add extensions/ast-tools/grep.ts extensions/ast-tools/grep.test.ts
git commit -m "feat(ast-tools): runAstGrep structural query"
```

---

## 任务 6：`runAstEdit` + 重写应用（napi 集成）

**文件：**
- 创建：`extensions/ast-tools/edit.ts`
- 测试：`extensions/ast-tools/edit.test.ts`

- [ ] **步骤 1：写失败的测试**

```ts
// edit.test.ts
import { describe, expect, it } from "vitest";
import { mkdtempSync, readFileSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { runAstEdit } from "./edit.js";

function fixture(content: string): { root: string; file: string } {
  const root = mkdtempSync(join(tmpdir(), "ast-edit-"));
  const file = join(root, "a.ts");
  writeFileSync(file, content);
  return { root, file };
}

describe("runAstEdit", () => {
  it("applies a single-metavariable rewrite and writes file", async () => {
    const { root, file } = fixture("console.log(1)\nconsole.log(2)\n");
    const res = await runAstEdit({
      ops: [{ pat: "console.log($A)", out: "logger.info($A)" }],
      paths: ["a.ts"], dryRun: false, cwd: root,
    });
    expect(res.totalReplacements).toBe(2);
    expect(res.files[0]).toMatchObject({ rel: "a.ts", replacements: 2 });
    expect(readFileSync(file, "utf8")).toBe("logger.info(1)\nlogger.info(2)\n");
  });
  it("dryRun does not write", async () => {
    const { root, file } = fixture("console.log(1)\n");
    const res = await runAstEdit({
      ops: [{ pat: "console.log($A)", out: "logger.info($A)" }],
      paths: ["a.ts"], dryRun: true, cwd: root,
    });
    expect(res.totalReplacements).toBe(1);
    expect(readFileSync(file, "utf8")).toBe("console.log(1)\n"); // 未改
  });
  it("applies multiple ops sequentially and reports zero matches", async () => {
    const { root, file } = fixture("a(1); b(2);\n");
    const res = await runAstEdit({
      ops: [{ pat: "a($X)", out: "A($X)" }, { pat: "nope($X)", out: "Z($X)" }],
      paths: ["a.ts"], dryRun: false, cwd: root,
    });
    expect(res.totalReplacements).toBe(1);
    expect(readFileSync(file, "utf8")).toBe("A(1); b(2);\n");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd extensions/ast-tools && npx vitest run edit.test.ts`
预期：FAIL，模块不存在。

- [ ] **步骤 3：实现 `edit.ts`**

```ts
// edit.ts
import { readFileSync, writeFileSync } from "node:fs";
import { parse, type SgNode } from "@ast-grep/napi";
import { collectFiles } from "./lang.js";
import { expandTemplate, type MetaResolver } from "./rewrite.js";

export interface AstEditOp { pat: string; out: string }
export interface AstEditFileResult { rel: string; replacements: number }
export interface AstEditResult {
  files: AstEditFileResult[];
  totalReplacements: number;
  filesSearched: number;
  applied: boolean;
  parseErrors: string[];
}
export interface AstEditArgs {
  ops: AstEditOp[];
  paths: string[];
  dryRun: boolean;
  cwd: string;
}

function buildResolver(node: SgNode, source: string): MetaResolver {
  return {
    single: (name) => node.getMatch(name)?.text() ?? null,
    multi: (name) => {
      const ns = node.getMultipleMatches(name);
      if (ns.length === 0) return null;
      const start = ns[0].range().start.index;
      const end = ns[ns.length - 1].range().end.index;
      return source.slice(start, end);
    },
  };
}

// 在单个文件源码上串行应用所有 op，返回 {next, count}。
function applyOpsToSource(source: string, lang: number, ops: AstEditOp[]): { next: string; count: number } {
  let current = source;
  let count = 0;
  for (const op of ops) {
    const root = parse(lang, current).root();
    const nodes = root.findAll(op.pat);
    if (nodes.length === 0) continue;
    const edits = nodes.map((n) => n.replace(expandTemplate(op.out, buildResolver(n, current))));
    edits.sort((a, b) => b.startPos - a.startPos); // 逆序应用，避免偏移错位
    current = root.commitEdits(edits);
    count += nodes.length;
  }
  return { next: current, count };
}

export async function runAstEdit(args: AstEditArgs): Promise<AstEditResult> {
  const files = await collectFiles(args.paths, args.cwd);
  const results: AstEditFileResult[] = [];
  const parseErrors: string[] = [];
  let total = 0;
  for (const f of files) {
    let src: string;
    try {
      src = readFileSync(f.abs, "utf8");
    } catch (err) {
      parseErrors.push(`${f.rel}: read failed ${(err as Error).message}`);
      continue;
    }
    let outcome: { next: string; count: number };
    try {
      outcome = applyOpsToSource(src, f.lang as unknown as number, args.ops);
    } catch (err) {
      parseErrors.push(`${f.rel}: ${(err as Error).message}`);
      continue;
    }
    if (outcome.count === 0) continue;
    if (!args.dryRun && outcome.next !== src) {
      try {
        writeFileSync(f.abs, outcome.next, "utf8");
      } catch (err) {
        parseErrors.push(`${f.rel}: write failed ${(err as Error).message}`);
        continue;
      }
    }
    results.push({ rel: f.rel, replacements: outcome.count });
    total += outcome.count;
  }
  return { files: results, totalReplacements: total, filesSearched: files.length, applied: !args.dryRun, parseErrors };
}
```

> 注：`f.lang` 是 `Lang`，`parse` 接受它；上面用 `as unknown as number` 仅为函数签名简化，实现时直接用 `f.lang` 传给 `parse` 即可（删掉 number 强转，签名改 `lang: Lang`）。

- [ ] **步骤 4：运行测试验证通过**

运行：`cd extensions/ast-tools && npx vitest run edit.test.ts`
预期：PASS（3 个用例）。若 `$$$` 切片分隔不符，回看 `buildResolver.multi` 的 index 切片。

- [ ] **步骤 5：Commit**

```bash
git add extensions/ast-tools/edit.ts extensions/ast-tools/edit.test.ts
git commit -m "feat(ast-tools): runAstEdit structural rewrite with metavar expansion"
```

---

## 任务 7：注册工具 + 聚合 + 整体 smoke

**文件：**
- 修改：`extensions/ast-tools/index.ts`
- 修改：`extensions/index.ts:5-36`（导入）、`:74-107`（allExtensions）

- [ ] **步骤 1：实现 `index.ts`（工具注册 + napi 降级）**

```ts
// index.ts
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
import { Type } from "typebox";
import { runAstGrep } from "./grep.js";
import { runAstEdit } from "./edit.js";

function napiAvailable(): boolean {
  try {
    // 触发 napi 原生加载；失败（缺 prebuilt）即降级。
    require("@ast-grep/napi");
    return true;
  } catch {
    return false;
  }
}

export default function (pi: ExtensionAPI) {
  if (!napiAvailable()) {
    console.error("[ast-tools] @ast-grep/napi 不可用，ast_grep/ast_edit 未注册");
    return;
  }
  console.error("[ast-tools] extension loaded");

  pi.registerTool({
    name: "ast_grep",
    label: "AST Search",
    description:
      "按语法结构（ast-grep pattern，如 `console.log($A)`）跨文件查询，比文本 grep 精确。" +
      "paths 支持文件/目录/glob；skip 分页。结构化重写用 ast_edit。",
    parameters: Type.Object({
      pat: Type.String({ description: "ast-grep pattern（元变量 $VAR / $$$ARGS）" }),
      paths: Type.Array(Type.String(), { description: "文件/目录/glob（>=1）", minItems: 1 }),
      skip: Type.Optional(Type.Number({ description: "跳过前 N 个匹配，默认 0" })),
    }),
    async execute(_id, params, _signal, _onUpdate, ctx) {
      const res = await runAstGrep({ pat: params.pat, paths: params.paths, skip: params.skip ?? 0, cwd: ctx.cwd });
      const lines = res.matches.map((m) => `${m.rel}:${m.line}:${m.column}  ${m.text.split("\n")[0]}`);
      const head = `${res.totalMatches} 个匹配（${res.filesSearched} 文件）` + (res.matches.length < res.totalMatches ? `，显示 ${res.matches.length}` : "");
      const errs = res.parseErrors.length ? `\n解析问题：\n- ${res.parseErrors.join("\n- ")}` : "";
      return { content: [{ type: "text", text: [head, ...lines].join("\n") + errs || "无匹配" }], details: { totalMatches: res.totalMatches } };
    },
  });

  pi.registerTool({
    name: "ast_edit",
    label: "AST Edit",
    description:
      "按语法结构批量重写：ops 每项 {pat, out}，out 模板里用 $VAR/$$$ARGS 引用 pat 捕获的内容。" +
      "默认直接写盘；dryRun=true 只报告不写。paths 支持文件/目录/glob。",
    parameters: Type.Object({
      ops: Type.Array(
        Type.Object({ pat: Type.String(), out: Type.String() }),
        { description: "重写对：pat 匹配，out 替换模板", minItems: 1 },
      ),
      paths: Type.Array(Type.String(), { description: "文件/目录/glob（>=1）", minItems: 1 }),
      dryRun: Type.Optional(Type.Boolean({ description: "true=只报告不写，默认 false" })),
    }),
    async execute(_id, params, _signal, _onUpdate, ctx) {
      const res = await runAstEdit({ ops: params.ops, paths: params.paths, dryRun: params.dryRun ?? false, cwd: ctx.cwd });
      const verb = res.applied ? "已改写" : "预览（未写盘）";
      const lines = res.files.map((f) => `${f.rel}: ${f.replacements}`);
      const errs = res.parseErrors.length ? `\n解析问题：\n- ${res.parseErrors.join("\n- ")}` : "";
      const body = res.totalReplacements === 0 ? "0 replacements（无匹配）" : `${verb}：共 ${res.totalReplacements} 处\n${lines.join("\n")}`;
      return { content: [{ type: "text", text: body + errs }], details: { totalReplacements: res.totalReplacements, applied: res.applied } };
    },
  });
}
```

- [ ] **步骤 2：聚合到 `extensions/index.ts`**

在导入区（约 `:5-36`）加：

```ts
import astTools from "./ast-tools/index.js";
```

在 `export { ... }`（约 `:38-71`）与 `allExtensions`（约 `:74-107`）数组里各加入 `astTools`（放在 `codeSearch` 附近，编码工具区）。

- [ ] **步骤 3：运行全部单测 + 类型/加载 smoke**

运行：`cd extensions/ast-tools && npx vitest run`
预期：lang/rewrite/grep/edit 全部 PASS。

运行：`cd extensions/ast-tools && npx jiti index.ts` （或 `node --import jiti/register index.ts`）
预期：打印 `[ast-tools] extension loaded`，无异常（验证 typebox schema 与 require 路径）。

- [ ] **步骤 4：Commit**

```bash
git add extensions/ast-tools/index.ts extensions/index.ts
git commit -m "feat(ast-tools): register ast_grep/ast_edit tools + aggregate"
```

---

## 自检

**1. 规格覆盖度（对照 `2026-06-17-ast-tools-design.md`）：**
- §4 schema `ast_grep{pat,paths,skip}` / `ast_edit{ops,paths,dryRun}` → 任务 5/6/7。
- §2 metavariable 不展开 → 任务 4 `expandTemplate` + 任务 6 `buildResolver`。
- §3 依赖 `@ast-grep/napi`、全量语言 → 任务 1/2。
- §5 组件 lang/grep/rewrite/edit/index → 任务 2/3/4/5/6/7。
- §6 并存不接管 → 任务 7 用独立工具名 `ast_grep`/`ast_edit`，不隐藏内置。
- §7 降级（napi 缺失/parse error/无匹配）→ 任务 7 `napiAvailable`、任务 5/6 `parseErrors`、`0 replacements`。
- §8 测试项 → 任务 2-6 测试覆盖。
- §9 非目标（internal-urls/resolve/.BLK）→ 未纳入任务，符合。

**2. 占位符扫描：** 无 TODO/待补；每步含完整代码与命令。任务 6 步骤3 的 `as unknown as number` 已用注释说明实现时改回 `Lang`。

**3. 类型一致性：** `CollectedFile{abs,rel,lang}`（任务3）被 grep/edit 消费（任务5/6）；`MetaResolver{single,multi}`（任务4）被 `buildResolver`（任务6）实现；`runAstGrep`/`runAstEdit` 返回类型与 index.ts 渲染（任务7）字段一致（`totalMatches`/`matches`/`totalReplacements`/`files`/`applied`/`parseErrors`）。

---

## 执行交接

计划已完成并保存到 `docs/superpowers/plans/2026-06-17-ast-tools.md`。两种执行方式：

1. **子代理驱动（推荐）** —— 每个任务调度一个新子代理，任务间审查，快速迭代（superpowers:subagent-driven-development）。
2. **内联执行** —— 当前会话用 superpowers:executing-plans 批量执行并设检查点。
