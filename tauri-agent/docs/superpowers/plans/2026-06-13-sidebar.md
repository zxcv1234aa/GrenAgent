# 侧边栏 / 会话列表（Codex 风跨项目）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 tauri-agent 的扁平单项目会话列表重构为 Codex 风格的跨项目侧边栏（顶部动作 + 置顶 + 按 cwd 项目分组 + ⋯菜单 + 仅活跃会话运行态）。

**架构：** 后端新增不过滤 cwd 的 `list_all_sessions`；前端按 cwd 聚合成项目组，叠加 localStorage 偏好（置顶/别名/隐藏/展开）；`activeWorkspace` 状态化，点击跨项目会话时 `open_workspace(cwd)+switch_session` 自动重建 agent store。

**技术栈：** React 19 + TypeScript + Vite + Tauri 2 (Rust) + zustand(+persist) + @lobehub/ui + antd Dropdown + lucide-react + antd-style + vitest。

参考规格：`tauri-agent/docs/superpowers/specs/2026-06-13-sidebar-design.md`

---

## 文件结构

**新增（前端 `tauri-agent/src/`）**
- `stores/sidebarPrefsStore.ts` — 持久化偏好（置顶/别名/隐藏/展开），zustand persist，key `pi-sidebar`
- `features/sessions/useProjectGroups.ts` — 把 `SessionInfo[]` 聚合为项目组并排序
- `features/sessions/useProjectMenu.ts` — 项目 ⋯/右键菜单项
- `features/sessions/useSessionMenu.ts` — 会话 ⋯/右键菜单项
- `features/sessions/RowActions.tsx` — 行尾 SquarePen + ⋯(Dropdown) 通用操作
- `features/sessions/SessionItem.tsx` — 会话行（标题 + 活跃运行态 + 内联重命名）
- `features/sessions/ProjectItem.tsx` — 项目头行（折叠箭头 + 文件夹 + 名称 + 徽标 + RowActions）
- `features/sessions/ProjectGroup.tsx` — 项目折叠组（头 + 会话列表 + 查看全部）
- `features/sessions/SidebarActions.tsx` — 新建 / 搜索入口
- `features/sessions/SearchBox.tsx` — 搜索输入
- `features/sessions/Sidebar.tsx` — 顶层容器装配
- 测试：上述各单元同目录 `*.test.ts(x)`

**修改**
- `src-tauri/src/commands/sessions.rs` — 新增 `list_all_sessions`
- `src-tauri/src/lib.rs` — 注册命令
- `src/lib/pi.ts` — 新增 `listAllSessions`、`setSessionName`
- `src/store/session.ts` — 扩展 `activeWorkspace`、`searchKeyword`、`allSessions`
- `src/App.tsx` — `WORKSPACE` 常量 → `activeWorkspace`；跨项目切换；挂 `Sidebar`
- 删除：`src/features/sessions/SessionList.tsx`（被 `Sidebar.tsx` 取代）

**验证命令**
- 前端类型：`npx tsc --noEmit`（工作目录 `tauri-agent`）
- 前端测试：`npx vitest run <file>`
- Rust 测试：`cargo test`（工作目录 `tauri-agent/src-tauri`）

---

## 任务 1：后端 `list_all_sessions`（不按 cwd 过滤）

**文件：**
- 修改：`tauri-agent/src-tauri/src/commands/sessions.rs`
- 修改：`tauri-agent/src-tauri/src/lib.rs`

- [ ] **步骤 1：在 `sessions.rs` 末尾追加命令**

```rust
/// 列出所有项目（cwd）的会话，按 timestamp 倒序，不做 cwd 过滤。
#[tauri::command]
pub async fn list_all_sessions() -> Result<Vec<SessionInfo>, String> {
    let dir = match sessions_dir() {
        Some(d) => d,
        None => return Ok(vec![]),
    };
    let mut files = Vec::new();
    collect_session_files(&dir, &mut files);

    let mut out = Vec::new();
    for path in files {
        if let Ok(contents) = read_first_line(&path) {
            let path_str = canonical_display_path(&path);
            if let Some(info) = parse_session_header(&contents, &path_str) {
                // 仅保留带 cwd 的会话（cwd 即"项目"，无 cwd 无法分组）
                if info.cwd.is_some() {
                    out.push(info);
                }
            }
        }
    }
    out.sort_by(|a, b| b.timestamp.cmp(&a.timestamp));
    Ok(out)
}
```

- [ ] **步骤 2：在 `sessions.rs` 的 `#[cfg(test)]` 模块加测试**

在已有测试模块内追加（复用其 `parse_session_header` 测试风格）：

```rust
#[test]
fn list_all_keeps_only_sessions_with_cwd() {
    // 纯逻辑校验 parse + 过滤条件：有 cwd 的保留，无 cwd 的丢弃
    let with_cwd = "{\"type\":\"session\",\"id\":\"a\",\"cwd\":\"/ws/a\",\"timestamp\":\"2026-06-10T00:00:00Z\"}\n";
    let no_cwd = "{\"type\":\"session\",\"id\":\"b\",\"timestamp\":\"2026-06-10T00:00:00Z\"}\n";
    assert!(parse_session_header(with_cwd, "/tmp/a.jsonl").unwrap().cwd.is_some());
    assert!(parse_session_header(no_cwd, "/tmp/b.jsonl").unwrap().cwd.is_none());
}
```

- [ ] **步骤 3：运行 Rust 测试确认通过**

运行：`cargo test list_all`（工作目录 `tauri-agent/src-tauri`）
预期：PASS

- [ ] **步骤 4：在 `lib.rs` 注册命令**

在 `tauri::generate_handler![...]` 列表里、`list_pi_sessions` 旁边加一行：

```rust
            commands::sessions::list_all_sessions,
```

（若 `list_pi_sessions` 以 `commands::list_pi_sessions` 形式引入，则按同样路径写 `commands::list_all_sessions`，与文件中现有写法保持一致。）

- [ ] **步骤 5：编译确认**

运行：`cargo check`（工作目录 `tauri-agent/src-tauri`）
预期：通过，无 "function not found in handler" 类报错。

- [ ] **步骤 6：Commit**

```bash
git add tauri-agent/src-tauri/src/commands/sessions.rs tauri-agent/src-tauri/src/lib.rs
git commit -m "feat(sidebar): add list_all_sessions backend command"
```

---

## 任务 2：前端 pi 绑定 `listAllSessions` / `setSessionName`

**文件：**
- 修改：`tauri-agent/src/lib/pi.ts`

- [ ] **步骤 1：在 `pi` 对象里新增两个绑定**

在 `listSessions` 之后追加：

```ts
  listAllSessions: () => invoke<SessionInfo[]>('list_all_sessions'),
  setSessionName: (workspace: string, name: string) =>
    invoke<unknown>('agent_set_session_name', { workspace, name }),
```

- [ ] **步骤 2：类型检查**

运行：`npx tsc --noEmit`（工作目录 `tauri-agent`）
预期：PASS（`SessionInfo` 已在本文件定义）

- [ ] **步骤 3：Commit**

```bash
git add tauri-agent/src/lib/pi.ts
git commit -m "feat(sidebar): bind listAllSessions and setSessionName IPC"
```

---

## 任务 3：`sidebarPrefsStore`（持久化偏好）

**文件：**
- 创建：`tauri-agent/src/stores/sidebarPrefsStore.ts`
- 测试：`tauri-agent/src/stores/sidebarPrefsStore.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
import { beforeEach, describe, expect, it } from 'vitest';
import { useSidebarPrefsStore } from './sidebarPrefsStore';

const reset = () =>
  useSidebarPrefsStore.setState({
    pinnedProjects: [],
    pinnedSessions: [],
    hiddenProjects: [],
    aliases: {},
    collapsed: {},
  });

describe('sidebarPrefsStore', () => {
  beforeEach(reset);

  it('toggles a pinned project', () => {
    const { togglePinnedProject } = useSidebarPrefsStore.getState();
    togglePinnedProject('/ws/a');
    expect(useSidebarPrefsStore.getState().pinnedProjects).toContain('/ws/a');
    togglePinnedProject('/ws/a');
    expect(useSidebarPrefsStore.getState().pinnedProjects).not.toContain('/ws/a');
  });

  it('sets and clears a project alias', () => {
    const { setAlias } = useSidebarPrefsStore.getState();
    setAlias('/ws/a', 'My Project');
    expect(useSidebarPrefsStore.getState().aliases['/ws/a']).toBe('My Project');
    setAlias('/ws/a', '');
    expect(useSidebarPrefsStore.getState().aliases['/ws/a']).toBeUndefined();
  });

  it('hides a project and toggles collapse (default collapsed=true)', () => {
    const s = useSidebarPrefsStore.getState();
    s.hideProject('/ws/b');
    expect(useSidebarPrefsStore.getState().hiddenProjects).toContain('/ws/b');
    expect(useSidebarPrefsStore.getState().isCollapsed('/ws/x', true)).toBe(true);
    s.toggleCollapsed('/ws/x', true);
    expect(useSidebarPrefsStore.getState().isCollapsed('/ws/x', true)).toBe(false);
  });
});
```

- [ ] **步骤 2：运行测试确认失败**

运行：`npx vitest run src/stores/sidebarPrefsStore.test.ts`
预期：FAIL（模块不存在）

- [ ] **步骤 3：实现 store**

```ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface SidebarPrefsState {
  pinnedProjects: string[]; // 按 cwd
  pinnedSessions: string[]; // 按 session.path
  hiddenProjects: string[]; // 按 cwd
  aliases: Record<string, string>; // cwd -> 别名
  collapsed: Record<string, boolean>; // cwd -> 是否折叠（仅存非默认值）

  togglePinnedProject: (cwd: string) => void;
  togglePinnedSession: (path: string) => void;
  hideProject: (cwd: string) => void;
  unhideProject: (cwd: string) => void;
  setAlias: (cwd: string, alias: string) => void;
  toggleCollapsed: (cwd: string, defaultCollapsed: boolean) => void;
  isCollapsed: (cwd: string, defaultCollapsed: boolean) => boolean;
}

const toggle = (arr: string[], v: string) =>
  arr.includes(v) ? arr.filter((x) => x !== v) : [...arr, v];

export const useSidebarPrefsStore = create<SidebarPrefsState>()(
  persist(
    (set, get) => ({
      pinnedProjects: [],
      pinnedSessions: [],
      hiddenProjects: [],
      aliases: {},
      collapsed: {},

      togglePinnedProject: (cwd) =>
        set((s) => ({ pinnedProjects: toggle(s.pinnedProjects, cwd) })),
      togglePinnedSession: (path) =>
        set((s) => ({ pinnedSessions: toggle(s.pinnedSessions, path) })),
      hideProject: (cwd) =>
        set((s) => ({ hiddenProjects: toggle(s.hiddenProjects.includes(cwd) ? s.hiddenProjects : [...s.hiddenProjects, cwd], '') .filter(Boolean) })),
      unhideProject: (cwd) =>
        set((s) => ({ hiddenProjects: s.hiddenProjects.filter((x) => x !== cwd) })),
      setAlias: (cwd, alias) =>
        set((s) => {
          const next = { ...s.aliases };
          if (alias.trim()) next[cwd] = alias.trim();
          else delete next[cwd];
          return { aliases: next };
        }),
      toggleCollapsed: (cwd, defaultCollapsed) =>
        set((s) => ({
          collapsed: { ...s.collapsed, [cwd]: !get().isCollapsed(cwd, defaultCollapsed) },
        })),
      isCollapsed: (cwd, defaultCollapsed) => {
        const v = get().collapsed[cwd];
        return v === undefined ? defaultCollapsed : v;
      },
    }),
    { name: 'pi-sidebar' },
  ),
);
```

> 注：`hideProject` 用简单去重 push 即可，上面的写法保持幂等。实现时若觉得绕，可改为
> `set((s) => ({ hiddenProjects: s.hiddenProjects.includes(cwd) ? s.hiddenProjects : [...s.hiddenProjects, cwd] }))`，等价。

- [ ] **步骤 4：运行测试确认通过**

运行：`npx vitest run src/stores/sidebarPrefsStore.test.ts`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add tauri-agent/src/stores/sidebarPrefsStore.ts tauri-agent/src/stores/sidebarPrefsStore.test.ts
git commit -m "feat(sidebar): add persisted sidebar prefs store"
```

---

## 任务 4：扩展 `useSessionStore`（activeWorkspace / 搜索 / 全量会话）

**文件：**
- 修改：`tauri-agent/src/store/session.ts`

- [ ] **步骤 1：替换 store 内容为扩展版**

```ts
import { create } from 'zustand';
import type { SessionInfo } from '../lib/pi';

interface SessionStore {
  sessions: SessionInfo[];        // 当前 workspace 的会话（保留，兼容现有用法）
  allSessions: SessionInfo[];     // 跨项目全量会话
  activeWorkspace: string;        // 当前选中项目 cwd（替代常量 WORKSPACE，默认 '.'）
  activeSessionPath: string | null;
  searchKeyword: string;
  isLoading: boolean;
  error: string | null;

  setSessions: (sessions: SessionInfo[]) => void;
  setAllSessions: (sessions: SessionInfo[]) => void;
  setActiveWorkspace: (cwd: string) => void;
  setActiveSession: (path: string) => void;
  setSearchKeyword: (kw: string) => void;
  setLoading: (loading: boolean) => void;
  setError: (error: string | null) => void;
}

export const useSessionStore = create<SessionStore>((set) => ({
  sessions: [],
  allSessions: [],
  activeWorkspace: '.',
  activeSessionPath: null,
  searchKeyword: '',
  isLoading: false,
  error: null,

  setSessions: (sessions) => set({ sessions }),
  setAllSessions: (allSessions) => set({ allSessions }),
  setActiveWorkspace: (activeWorkspace) => set({ activeWorkspace }),
  setActiveSession: (path) => set({ activeSessionPath: path }),
  setSearchKeyword: (searchKeyword) => set({ searchKeyword }),
  setLoading: (isLoading) => set({ isLoading }),
  setError: (error) => set({ error }),
}));
```

- [ ] **步骤 2：类型检查**

运行：`npx tsc --noEmit`（工作目录 `tauri-agent`）
预期：PASS（仅新增字段，旧字段保留，不破坏现有引用）

- [ ] **步骤 3：Commit**

```bash
git add tauri-agent/src/store/session.ts
git commit -m "feat(sidebar): extend session store with activeWorkspace and search"
```

---

## 任务 5：`useProjectGroups`（聚合 + 排序）

**文件：**
- 创建：`tauri-agent/src/features/sessions/useProjectGroups.ts`
- 测试：`tauri-agent/src/features/sessions/useProjectGroups.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from 'vitest';
import type { SessionInfo } from '../../lib/pi';
import { buildProjectGroups } from './useProjectGroups';

const s = (id: string, cwd: string, ts: string, name?: string): SessionInfo => ({
  id, path: `/sess/${id}.jsonl`, cwd, timestamp: ts, name: name ?? null,
});

describe('buildProjectGroups', () => {
  it('groups by cwd and sorts sessions desc by timestamp', () => {
    const groups = buildProjectGroups(
      [s('a', '/ws/p1', '2026-06-10T01:00:00Z'), s('b', '/ws/p1', '2026-06-10T03:00:00Z')],
      { current: '/ws/p1', pinnedProjects: [], hiddenProjects: [], aliases: {}, keyword: '' },
    );
    expect(groups).toHaveLength(1);
    expect(groups[0].sessions.map((x) => x.id)).toEqual(['b', 'a']);
  });

  it('puts current project first, then projects by most-recent activity', () => {
    const groups = buildProjectGroups(
      [s('a', '/ws/old', '2026-06-01T00:00:00Z'), s('b', '/ws/p1', '2026-06-10T00:00:00Z'), s('c', '/ws/new', '2026-06-12T00:00:00Z')],
      { current: '/ws/p1', pinnedProjects: [], hiddenProjects: [], aliases: {}, keyword: '' },
    );
    expect(groups.map((g) => g.cwd)).toEqual(['/ws/p1', '/ws/new', '/ws/old']);
    expect(groups[0].isCurrent).toBe(true);
  });

  it('marks pinned and excludes hidden; keyword filters by name/project', () => {
    const base = [s('a', '/ws/p1', '2026-06-10T00:00:00Z', 'Login fix'), s('b', '/ws/p2', '2026-06-11T00:00:00Z', 'Theme')];
    const pinned = buildProjectGroups(base, { current: '/ws/p1', pinnedProjects: ['/ws/p2'], hiddenProjects: [], aliases: {}, keyword: '' });
    expect(pinned.find((g) => g.cwd === '/ws/p2')!.pinned).toBe(true);

    const hidden = buildProjectGroups(base, { current: '/ws/p1', pinnedProjects: [], hiddenProjects: ['/ws/p2'], aliases: {}, keyword: '' });
    expect(hidden.find((g) => g.cwd === '/ws/p2')).toBeUndefined();

    const filtered = buildProjectGroups(base, { current: '/ws/p1', pinnedProjects: [], hiddenProjects: [], aliases: {}, keyword: 'theme' });
    expect(filtered.map((g) => g.cwd)).toEqual(['/ws/p2']);
  });

  it('derives display name from alias or cwd basename', () => {
    const groups = buildProjectGroups(
      [s('a', '/ws/my-proj', '2026-06-10T00:00:00Z')],
      { current: '/ws/my-proj', pinnedProjects: [], hiddenProjects: [], aliases: { '/ws/my-proj': 'Alias' }, keyword: '' },
    );
    expect(groups[0].name).toBe('Alias');
  });
});
```

- [ ] **步骤 2：运行测试确认失败**

运行：`npx vitest run src/features/sessions/useProjectGroups.test.ts`
预期：FAIL（`buildProjectGroups` 未定义）

- [ ] **步骤 3：实现纯函数 + hook**

```ts
import { useMemo } from 'react';
import type { SessionInfo } from '../../lib/pi';
import { useSessionStore } from '../../store/session';
import { useSidebarPrefsStore } from '../../stores/sidebarPrefsStore';

export interface ProjectGroup {
  cwd: string;
  name: string;
  isCurrent: boolean;
  pinned: boolean;
  sessions: SessionInfo[];
  lastActivity: string; // 该组最新 timestamp
}

interface BuildParams {
  current: string;
  pinnedProjects: string[];
  hiddenProjects: string[];
  aliases: Record<string, string>;
  keyword: string;
}

const basename = (p: string) => p.replace(/[\\/]+$/, '').split(/[\\/]/).pop() || p;

export function buildProjectGroups(sessions: SessionInfo[], params: BuildParams): ProjectGroup[] {
  const { current, pinnedProjects, hiddenProjects, aliases, keyword } = params;
  const kw = keyword.trim().toLowerCase();

  const byCwd = new Map<string, SessionInfo[]>();
  for (const s of sessions) {
    if (!s.cwd) continue;
    if (!byCwd.has(s.cwd)) byCwd.set(s.cwd, []);
    byCwd.get(s.cwd)!.push(s);
  }

  let groups: ProjectGroup[] = [];
  for (const [cwd, list] of byCwd) {
    if (hiddenProjects.includes(cwd)) continue;
    const sorted = [...list].sort((a, b) => (b.timestamp ?? '').localeCompare(a.timestamp ?? ''));
    const name = aliases[cwd] || basename(cwd);
    groups.push({
      cwd,
      name,
      isCurrent: cwd === current,
      pinned: pinnedProjects.includes(cwd),
      sessions: sorted,
      lastActivity: sorted[0]?.timestamp ?? '',
    });
  }

  // 关键字过滤：项目名命中 或 任一会话标题命中
  if (kw) {
    groups = groups
      .map((g) => {
        const nameHit = g.name.toLowerCase().includes(kw);
        const hitSessions = g.sessions.filter((s) => (s.name ?? '').toLowerCase().includes(kw));
        if (nameHit) return g;
        if (hitSessions.length) return { ...g, sessions: hitSessions };
        return null;
      })
      .filter((g): g is ProjectGroup => g !== null);
  }

  // 排序：当前项目 > 置顶 > 最近活跃
  groups.sort((a, b) => {
    if (a.isCurrent !== b.isCurrent) return a.isCurrent ? -1 : 1;
    if (a.pinned !== b.pinned) return a.pinned ? -1 : 1;
    return (b.lastActivity ?? '').localeCompare(a.lastActivity ?? '');
  });

  return groups;
}

export function useProjectGroups(): ProjectGroup[] {
  const allSessions = useSessionStore((s) => s.allSessions);
  const current = useSessionStore((s) => s.activeWorkspace);
  const keyword = useSessionStore((s) => s.searchKeyword);
  const pinnedProjects = useSidebarPrefsStore((s) => s.pinnedProjects);
  const hiddenProjects = useSidebarPrefsStore((s) => s.hiddenProjects);
  const aliases = useSidebarPrefsStore((s) => s.aliases);

  return useMemo(
    () => buildProjectGroups(allSessions, { current, pinnedProjects, hiddenProjects, aliases, keyword }),
    [allSessions, current, pinnedProjects, hiddenProjects, aliases, keyword],
  );
}
```

- [ ] **步骤 4：运行测试确认通过**

运行：`npx vitest run src/features/sessions/useProjectGroups.test.ts`
预期：PASS（全部 4 个用例）

- [ ] **步骤 5：Commit**

```bash
git add tauri-agent/src/features/sessions/useProjectGroups.ts tauri-agent/src/features/sessions/useProjectGroups.test.ts
git commit -m "feat(sidebar): add useProjectGroups aggregation"
```

---

## 任务 6：菜单 hooks `useSessionMenu` / `useProjectMenu`

**文件：**
- 创建：`tauri-agent/src/features/sessions/useSessionMenu.ts`
- 创建：`tauri-agent/src/features/sessions/useProjectMenu.ts`
- 测试：`tauri-agent/src/features/sessions/sessionMenu.test.ts`

菜单项类型用 antd `MenuProps['items']`（`{ key, label, icon, danger, onClick }`）。

- [ ] **步骤 1：编写失败的测试（纯构造函数）**

```ts
import { describe, expect, it, vi } from 'vitest';
import { buildSessionMenuItems } from './useSessionMenu';

describe('buildSessionMenuItems', () => {
  it('shows pin when not pinned, unpin when pinned', () => {
    const a = buildSessionMenuItems({ pinned: false, onPinToggle: vi.fn(), onRename: vi.fn(), onDelete: vi.fn() });
    expect(a.find((i) => i!.key === 'pin')).toBeTruthy();
    const b = buildSessionMenuItems({ pinned: true, onPinToggle: vi.fn(), onRename: vi.fn(), onDelete: vi.fn() });
    expect(b.find((i) => i!.key === 'pin')!.label).toContain('取消置顶');
  });

  it('delete item is marked danger and fires callback', () => {
    const onDelete = vi.fn();
    const items = buildSessionMenuItems({ pinned: false, onPinToggle: vi.fn(), onRename: vi.fn(), onDelete });
    const del = items.find((i) => i!.key === 'delete') as { danger?: boolean; onClick?: () => void };
    expect(del.danger).toBe(true);
    del.onClick!();
    expect(onDelete).toHaveBeenCalledOnce();
  });
});
```

- [ ] **步骤 2：运行确认失败**

运行：`npx vitest run src/features/sessions/sessionMenu.test.ts`
预期：FAIL

- [ ] **步骤 3：实现 `useSessionMenu.ts`**

```ts
import type { MenuProps } from 'antd';
import { createElement } from 'react';
import { Icon } from '@lobehub/ui';
import { PencilLine, Pin, PinOff, Trash2 } from 'lucide-react';

export interface SessionMenuParams {
  pinned: boolean;
  onPinToggle: () => void;
  onRename: () => void;
  onDelete: () => void;
}

type Items = NonNullable<MenuProps['items']>;

export function buildSessionMenuItems(p: SessionMenuParams): Items {
  return [
    {
      key: 'pin',
      icon: createElement(Icon, { icon: p.pinned ? PinOff : Pin, size: 'small' }),
      label: p.pinned ? '取消置顶' : '置顶',
      onClick: p.onPinToggle,
    },
    {
      key: 'rename',
      icon: createElement(Icon, { icon: PencilLine, size: 'small' }),
      label: '重命名',
      onClick: p.onRename,
    },
    { type: 'divider' },
    {
      key: 'delete',
      icon: createElement(Icon, { icon: Trash2, size: 'small' }),
      label: '删除',
      danger: true,
      onClick: p.onDelete,
    },
  ];
}

export const useSessionMenu = (p: SessionMenuParams): Items => buildSessionMenuItems(p);
```

- [ ] **步骤 4：实现 `useProjectMenu.ts`**

```ts
import type { MenuProps } from 'antd';
import { createElement } from 'react';
import { Icon } from '@lobehub/ui';
import { EyeOff, FolderOpen, PencilLine, Pin, PinOff } from 'lucide-react';

export interface ProjectMenuParams {
  pinned: boolean;
  onPinToggle: () => void;
  onReveal: () => void;   // 在资源管理器中打开
  onRename: () => void;   // 重命名（别名）
  onHide: () => void;     // 从列表隐藏
}

type Items = NonNullable<MenuProps['items']>;

export function buildProjectMenuItems(p: ProjectMenuParams): Items {
  return [
    {
      key: 'pin',
      icon: createElement(Icon, { icon: p.pinned ? PinOff : Pin, size: 'small' }),
      label: p.pinned ? '取消置顶项目' : '置顶项目',
      onClick: p.onPinToggle,
    },
    {
      key: 'reveal',
      icon: createElement(Icon, { icon: FolderOpen, size: 'small' }),
      label: '在资源管理器中打开',
      onClick: p.onReveal,
    },
    {
      key: 'rename',
      icon: createElement(Icon, { icon: PencilLine, size: 'small' }),
      label: '重命名（别名）',
      onClick: p.onRename,
    },
    { type: 'divider' },
    {
      key: 'hide',
      icon: createElement(Icon, { icon: EyeOff, size: 'small' }),
      label: '从列表隐藏',
      danger: true,
      onClick: p.onHide,
    },
  ];
}

export const useProjectMenu = (p: ProjectMenuParams): Items => buildProjectMenuItems(p);
```

> "在终端打开" 后端暂无命令，按规格延后到二期，本计划不实现，避免占位。

- [ ] **步骤 5：运行测试确认通过**

运行：`npx vitest run src/features/sessions/sessionMenu.test.ts`
预期：PASS

- [ ] **步骤 6：Commit**

```bash
git add tauri-agent/src/features/sessions/useSessionMenu.ts tauri-agent/src/features/sessions/useProjectMenu.ts tauri-agent/src/features/sessions/sessionMenu.test.ts
git commit -m "feat(sidebar): add session/project menu builders"
```

---

## 任务 7：`RowActions`（SquarePen + ⋯ Dropdown）

**文件：**
- 创建：`tauri-agent/src/features/sessions/RowActions.tsx`

- [ ] **步骤 1：实现组件**

```tsx
import type { MenuProps } from 'antd';
import { Dropdown } from 'antd';
import { ActionIcon } from '@lobehub/ui';
import { MoreHorizontal, SquarePen } from 'lucide-react';

interface RowActionsProps {
  menuItems: NonNullable<MenuProps['items']>;
  onNew?: () => void; // 有则显示"在此项目新建会话"
}

export function RowActions({ menuItems, onNew }: RowActionsProps) {
  return (
    <span style={{ display: 'flex', gap: 2 }} onClick={(e) => e.stopPropagation()}>
      {onNew && (
        <ActionIcon icon={SquarePen} size="small" title="在此项目新建会话" onClick={onNew} />
      )}
      <Dropdown menu={{ items: menuItems }} trigger={['click']}>
        <ActionIcon icon={MoreHorizontal} size="small" title="更多" />
      </Dropdown>
    </span>
  );
}
```

- [ ] **步骤 2：类型检查**

运行：`npx tsc --noEmit`
预期：PASS

- [ ] **步骤 3：Commit**

```bash
git add tauri-agent/src/features/sessions/RowActions.tsx
git commit -m "feat(sidebar): add RowActions (new + more dropdown)"
```

---

## 任务 8：`SessionItem`（标题 + 活跃运行态 + 内联重命名 + 右键菜单）

**文件：**
- 创建：`tauri-agent/src/features/sessions/SessionItem.tsx`
- 测试：`tauri-agent/src/features/sessions/SessionItem.test.tsx`

- [ ] **步骤 1：编写失败的测试**

```tsx
import { fireEvent, render, screen } from '@testing-library/react';
import { describe, expect, it, vi } from 'vitest';
import { SessionItem } from './SessionItem';

const base = {
  title: '修复登录 bug',
  active: false,
  running: false,
  pinned: false,
  onClick: vi.fn(),
  onPinToggle: vi.fn(),
  onRename: vi.fn(),
  onDelete: vi.fn(),
};

describe('SessionItem', () => {
  it('renders title and fires onClick', () => {
    const onClick = vi.fn();
    render(<SessionItem {...base} onClick={onClick} />);
    fireEvent.click(screen.getByText('修复登录 bug'));
    expect(onClick).toHaveBeenCalledOnce();
  });

  it('shows running indicator only when running', () => {
    const { rerender } = render(<SessionItem {...base} running={false} />);
    expect(screen.queryByTestId('session-running')).toBeNull();
    rerender(<SessionItem {...base} running />);
    expect(screen.getByTestId('session-running')).toBeTruthy();
  });

  it('enters inline edit and submits new name on Enter', () => {
    const onRename = vi.fn();
    render(<SessionItem {...base} onRename={onRename} editing />);
    const input = screen.getByTestId('session-rename-input') as HTMLInputElement;
    fireEvent.change(input, { target: { value: '新标题' } });
    fireEvent.keyDown(input, { key: 'Enter' });
    expect(onRename).toHaveBeenCalledWith('新标题');
  });
});
```

- [ ] **步骤 2：运行确认失败**

运行：`npx vitest run src/features/sessions/SessionItem.test.tsx`
预期：FAIL

- [ ] **步骤 3：实现组件**

```tsx
import { useEffect, useRef, useState } from 'react';
import { Input } from 'antd';
import { ActionIcon, Icon } from '@lobehub/ui';
import { createStyles } from 'antd-style';
import { Hand, LoaderCircle } from 'lucide-react';
import { Dropdown } from 'antd';
import { MoreHorizontal } from 'lucide-react';
import { buildSessionMenuItems } from './useSessionMenu';

const useStyles = createStyles(({ token, css }) => ({
  row: css`
    display: flex; align-items: center; gap: 8px;
    height: 28px; margin: 0 6px; padding: 0 8px 0 16px;
    border-radius: 7px; color: ${token.colorTextSecondary}; cursor: pointer;
    &:hover { background: ${token.colorFillTertiary}; }
  `,
  active: css`
    background: ${token.colorFill}; color: ${token.colorText};
    &:hover { background: ${token.colorFill}; }
  `,
  title: css`flex: 1; overflow: hidden; white-space: nowrap; text-overflow: ellipsis;`,
  spin: css`display: inline-flex; color: ${token.colorWarning};
    animation: sidebarSpin 1.1s linear infinite;
    @keyframes sidebarSpin { to { transform: rotate(360deg); } }`,
  acts: css`display: none; .lobe-session-row:hover & { display: flex; }`,
}));

export interface SessionItemProps {
  title: string;
  active: boolean;
  running: boolean;
  waiting?: boolean;
  pinned: boolean;
  editing?: boolean;
  onClick: () => void;
  onPinToggle: () => void;
  onRename: (name: string) => void;
  onDelete: () => void;
}

export function SessionItem(props: SessionItemProps) {
  const { title, active, running, waiting, pinned, editing, onClick, onPinToggle, onRename, onDelete } = props;
  const { styles, cx } = useStyles();
  const [draft, setDraft] = useState(title);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    if (editing) {
      setDraft(title);
      inputRef.current?.focus();
    }
  }, [editing, title]);

  const submit = () => {
    const v = draft.trim();
    if (v && v !== title) onRename(v);
    else onRename(title); // 空或未变 → 回退原名（由调用方决定是否落库）
  };

  const items = buildSessionMenuItems({
    pinned,
    onPinToggle,
    onRename: () => onRename(title), // 触发进入编辑由上层 editing 控制；此处占位为重新进入
    onDelete,
  });

  if (editing) {
    return (
      <div className={cx(styles.row)}>
        <Input
          ref={inputRef as never}
          data-testid="session-rename-input"
          size="small"
          defaultValue={title}
          onChange={(e) => setDraft(e.target.value)}
          onPressEnter={submit}
          onBlur={submit}
          onKeyDown={(e) => { if (e.key === 'Escape') onRename(title); }}
        />
      </div>
    );
  }

  return (
    <div className={cx('lobe-session-row', styles.row, active && styles.active)} onClick={onClick}>
      <span className={styles.title}>{title}</span>
      {running && (
        <span data-testid="session-running" className={styles.spin}>
          <Icon icon={waiting ? Hand : LoaderCircle} size="small" />
        </span>
      )}
      <span className={styles.acts} onClick={(e) => e.stopPropagation()}>
        <Dropdown menu={{ items }} trigger={['click']}>
          <ActionIcon icon={MoreHorizontal} size="small" title="更多" />
        </Dropdown>
      </span>
    </div>
  );
}
```

> 注：`onRename` 在菜单里点击"重命名"应由上层把该会话切到 `editing` 态（任务 9/11 用 `renamingPath` 控制）。组件内 `editing` 为受控 prop。测试用 `editing` 直接驱动输入框。

- [ ] **步骤 4：运行测试确认通过**

运行：`npx vitest run src/features/sessions/SessionItem.test.tsx`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add tauri-agent/src/features/sessions/SessionItem.tsx tauri-agent/src/features/sessions/SessionItem.test.tsx
git commit -m "feat(sidebar): add SessionItem with run state and inline rename"
```

---

## 任务 9：`ProjectItem` + `ProjectGroup`

**文件：**
- 创建：`tauri-agent/src/features/sessions/ProjectItem.tsx`
- 创建：`tauri-agent/src/features/sessions/ProjectGroup.tsx`

- [ ] **步骤 1：实现 `ProjectItem.tsx`**

```tsx
import { Dropdown } from 'antd';
import { ActionIcon, Icon } from '@lobehub/ui';
import { createStyles } from 'antd-style';
import {
  ChevronDown, ChevronRight, FolderClosed, FolderOpen, MoreHorizontal, SquarePen,
} from 'lucide-react';
import { buildProjectMenuItems } from './useProjectMenu';

const useStyles = createStyles(({ token, css }) => ({
  row: css`
    display: flex; align-items: center; gap: 8px;
    height: 30px; margin: 0 6px; padding: 0 8px 0 10px;
    border-radius: 7px; cursor: pointer; color: ${token.colorText};
    &:hover { background: ${token.colorFillTertiary}; }
  `,
  car: css`color: ${token.colorTextQuaternary}; display: inline-flex;`,
  name: css`flex: 1; overflow: hidden; white-space: nowrap; text-overflow: ellipsis;`,
  badge: css`font-size: 10px; color: ${token.colorTextTertiary};
    border: 1px solid ${token.colorBorderSecondary}; border-radius: 4px; padding: 0 5px;`,
  acts: css`display: none; .lobe-proj-row:hover & { display: flex; gap: 2px; }`,
}));

export interface ProjectItemProps {
  name: string;
  expanded: boolean;
  isCurrent: boolean;
  pinned: boolean;
  onToggle: () => void;
  onNew: () => void;
  onPinToggle: () => void;
  onReveal: () => void;
  onRename: () => void;
  onHide: () => void;
}

export function ProjectItem(p: ProjectItemProps) {
  const { styles, cx } = useStyles();
  const items = buildProjectMenuItems({
    pinned: p.pinned, onPinToggle: p.onPinToggle, onReveal: p.onReveal, onRename: p.onRename, onHide: p.onHide,
  });
  return (
    <Dropdown menu={{ items }} trigger={['contextMenu']}>
      <div className={cx('lobe-proj-row', styles.row)} onClick={p.onToggle}>
        <span className={styles.car}>
          <Icon icon={p.expanded ? ChevronDown : ChevronRight} size="small" />
        </span>
        <Icon icon={p.expanded ? FolderOpen : FolderClosed} size="small" />
        <span className={styles.name}>{p.name}</span>
        {p.isCurrent && <span className={styles.badge}>当前</span>}
        <span className={styles.acts} onClick={(e) => e.stopPropagation()}>
          <ActionIcon icon={SquarePen} size="small" title="在此项目新建会话" onClick={p.onNew} />
          <Dropdown menu={{ items }} trigger={['click']}>
            <ActionIcon icon={MoreHorizontal} size="small" title="更多" />
          </Dropdown>
        </span>
      </div>
    </Dropdown>
  );
}
```

- [ ] **步骤 2：实现 `ProjectGroup.tsx`**

```tsx
import { useState } from 'react';
import { Icon } from '@lobehub/ui';
import { createStyles } from 'antd-style';
import { ChevronDown } from 'lucide-react';
import type { ProjectGroup as Group } from './useProjectGroups';
import { ProjectItem } from './ProjectItem';
import { SessionItem } from './SessionItem';

const DEFAULT_VISIBLE = 5;

const useStyles = createStyles(({ token, css }) => ({
  more: css`
    display: flex; align-items: center; gap: 5px;
    margin: 0 6px; padding: 2px 10px 4px 28px;
    font-size: 12px; color: ${token.colorTextTertiary}; cursor: pointer;
    &:hover { color: ${token.colorText}; }
  `,
}));

export interface ProjectGroupProps {
  group: Group;
  expanded: boolean;
  activeSessionPath: string | null;
  runningSessionPath: string | null;
  renamingPath: string | null;
  onToggleExpand: () => void;
  onNewInProject: (cwd: string) => void;
  onPinProject: (cwd: string) => void;
  onRevealProject: (cwd: string) => void;
  onRenameProject: (cwd: string) => void;
  onHideProject: (cwd: string) => void;
  onOpenSession: (cwd: string, path: string) => void;
  onPinSession: (path: string) => void;
  onRequestRename: (path: string) => void;
  onSubmitRename: (path: string, name: string) => void;
  onDeleteSession: (cwd: string, path: string) => void;
  isSessionPinned: (path: string) => boolean;
}

export function ProjectGroup(p: ProjectGroupProps) {
  const { styles } = useStyles();
  const [showAll, setShowAll] = useState(false);
  const g = p.group;
  const visible = showAll ? g.sessions : g.sessions.slice(0, DEFAULT_VISIBLE);
  const hiddenCount = g.sessions.length - visible.length;

  return (
    <>
      <ProjectItem
        name={g.name}
        expanded={p.expanded}
        isCurrent={g.isCurrent}
        pinned={g.pinned}
        onToggle={p.onToggleExpand}
        onNew={() => p.onNewInProject(g.cwd)}
        onPinToggle={() => p.onPinProject(g.cwd)}
        onReveal={() => p.onRevealProject(g.cwd)}
        onRename={() => p.onRenameProject(g.cwd)}
        onHide={() => p.onHideProject(g.cwd)}
      />
      {p.expanded &&
        visible.map((s) => (
          <SessionItem
            key={s.path}
            title={s.name || 'Untitled'}
            active={p.activeSessionPath === s.path}
            running={p.runningSessionPath === s.path}
            pinned={p.isSessionPinned(s.path)}
            editing={p.renamingPath === s.path}
            onClick={() => p.onOpenSession(g.cwd, s.path)}
            onPinToggle={() => p.onPinSession(s.path)}
            onRename={(name) => p.onSubmitRename(s.path, name)}
            onDelete={() => p.onDeleteSession(g.cwd, s.path)}
          />
        ))}
      {p.expanded && hiddenCount > 0 && (
        <div className={styles.more} onClick={() => setShowAll(true)}>
          <Icon icon={ChevronDown} size="small" /> 查看全部 {g.sessions.length} 条
        </div>
      )}
    </>
  );
}
```

- [ ] **步骤 3：类型检查**

运行：`npx tsc --noEmit`
预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add tauri-agent/src/features/sessions/ProjectItem.tsx tauri-agent/src/features/sessions/ProjectGroup.tsx
git commit -m "feat(sidebar): add ProjectItem and ProjectGroup"
```

---

## 任务 10：`SidebarActions` + `SearchBox`

**文件：**
- 创建：`tauri-agent/src/features/sessions/SidebarActions.tsx`
- 创建：`tauri-agent/src/features/sessions/SearchBox.tsx`

- [ ] **步骤 1：实现 `SearchBox.tsx`**

```tsx
import { Input } from 'antd';
import { Icon } from '@lobehub/ui';
import { Search } from 'lucide-react';
import { useSessionStore } from '../../store/session';

export function SearchBox() {
  const keyword = useSessionStore((s) => s.searchKeyword);
  const setKeyword = useSessionStore((s) => s.setSearchKeyword);
  return (
    <Input
      allowClear
      size="small"
      placeholder="搜索会话 / 项目"
      prefix={<Icon icon={Search} size="small" />}
      value={keyword}
      onChange={(e) => setKeyword(e.target.value)}
      style={{ margin: '0 8px 4px', width: 'calc(100% - 16px)' }}
    />
  );
}
```

- [ ] **步骤 2：实现 `SidebarActions.tsx`**

```tsx
import { useState } from 'react';
import { Icon } from '@lobehub/ui';
import { createStyles } from 'antd-style';
import { MessageSquarePlus, Search } from 'lucide-react';
import { SearchBox } from './SearchBox';

const useStyles = createStyles(({ token, css }) => ({
  wrap: css`padding: 8px 8px 4px; display: flex; flex-direction: column; gap: 2px;`,
  act: css`
    height: 32px; display: flex; align-items: center; gap: 10px;
    padding: 0 10px; border-radius: 7px; cursor: pointer; color: ${token.colorText};
    &:hover { background: ${token.colorFillTertiary}; }
  `,
}));

interface SidebarActionsProps {
  onNew: () => void;
}

export function SidebarActions({ onNew }: SidebarActionsProps) {
  const { styles } = useStyles();
  const [searching, setSearching] = useState(false);
  return (
    <div className={styles.wrap}>
      <div className={styles.act} onClick={onNew}>
        <Icon icon={MessageSquarePlus} size="small" /> 新建会话
      </div>
      {searching ? (
        <SearchBox />
      ) : (
        <div className={styles.act} onClick={() => setSearching(true)}>
          <Icon icon={Search} size="small" /> 搜索会话
        </div>
      )}
    </div>
  );
}
```

- [ ] **步骤 3：类型检查**

运行：`npx tsc --noEmit`
预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add tauri-agent/src/features/sessions/SidebarActions.tsx tauri-agent/src/features/sessions/SearchBox.tsx
git commit -m "feat(sidebar): add SidebarActions and SearchBox"
```

---

## 任务 11：`Sidebar` 容器装配

**文件：**
- 创建：`tauri-agent/src/features/sessions/Sidebar.tsx`

- [ ] **步骤 1：实现容器**

```tsx
import { useState } from 'react';
import { Flexbox, ActionIcon, Icon, Empty } from '@lobehub/ui';
import { createStyles } from 'antd-style';
import { PanelLeftClose, Settings } from 'lucide-react';
import { openPath } from '@tauri-apps/plugin-opener';
import { PanelHeader } from '../../components/PanelHeader';
import { useSessionStore } from '../../store/session';
import { useSidebarPrefsStore } from '../../stores/sidebarPrefsStore';
import { useProjectGroups } from './useProjectGroups';
import { SidebarActions } from './SidebarActions';
import { ProjectGroup } from './ProjectGroup';

const useStyles = createStyles(({ token, css }) => ({
  sec: css`padding: 12px 14px 4px; font-size: 10px; letter-spacing: .09em;
    text-transform: uppercase; color: ${token.colorTextTertiary};`,
  scroll: css`flex: 1; min-height: 0; overflow-y: auto;`,
  foot: css`border-top: 1px solid ${token.colorBorderSecondary};
    padding: 8px 10px; display: flex; align-items: center; justify-content: space-between;`,
  footL: css`display: flex; align-items: center; gap: 8px; cursor: pointer; color: ${token.colorText};`,
}));

export interface SidebarProps {
  runningSessionPath: string | null;
  onNewSession: (cwd: string) => void;
  onOpenSession: (cwd: string, path: string) => void;
  onDeleteSession: (cwd: string, path: string) => void;
  onSubmitRename: (cwd: string, path: string, name: string) => void;
  onToggleSidebar: () => void;
  onOpenSettings: () => void;
}

export function Sidebar(props: SidebarProps) {
  const { styles } = useStyles();
  const groups = useProjectGroups();
  const activeSessionPath = useSessionStore((s) => s.activeSessionPath);
  const activeWorkspace = useSessionStore((s) => s.activeWorkspace);
  const prefs = useSidebarPrefsStore();
  const [renamingPath, setRenamingPath] = useState<string | null>(null);

  const pinnedGroups = groups.filter((g) => g.pinned);
  const normalGroups = groups.filter((g) => !g.pinned);

  const renderGroup = (g: (typeof groups)[number]) => (
    <ProjectGroup
      key={g.cwd}
      group={g}
      expanded={!prefs.isCollapsed(g.cwd, !g.isCurrent)}
      activeSessionPath={activeSessionPath}
      runningSessionPath={props.runningSessionPath}
      renamingPath={renamingPath}
      onToggleExpand={() => prefs.toggleCollapsed(g.cwd, !g.isCurrent)}
      onNewInProject={props.onNewSession}
      onPinProject={prefs.togglePinnedProject}
      onRevealProject={(cwd) => void openPath(cwd)}
      onRenameProject={(cwd) => {
        const next = window.prompt('项目别名', g.name);
        if (next !== null) prefs.setAlias(cwd, next);
      }}
      onHideProject={prefs.hideProject}
      onOpenSession={props.onOpenSession}
      onPinSession={prefs.togglePinnedSession}
      onRequestRename={setRenamingPath}
      onSubmitRename={(path, name) => {
        setRenamingPath(null);
        props.onSubmitRename(g.cwd, path, name);
      }}
      onDeleteSession={props.onDeleteSession}
      isSessionPinned={(path) => prefs.pinnedSessions.includes(path)}
    />
  );

  return (
    <Flexbox height="100%" style={{ minHeight: 0 }}>
      <PanelHeader
        title="Pi Agent"
        actions={<ActionIcon icon={PanelLeftClose} title="收起" onClick={props.onToggleSidebar} />}
      />
      <SidebarActions onNew={() => props.onNewSession(activeWorkspace)} />
      <div className={styles.scroll}>
        {groups.length === 0 && <Empty description="暂无会话" />}
        {pinnedGroups.length > 0 && <div className={styles.sec}>置顶</div>}
        {pinnedGroups.map(renderGroup)}
        {normalGroups.length > 0 && <div className={styles.sec}>项目</div>}
        {normalGroups.map(renderGroup)}
      </div>
      <div className={styles.foot}>
        <span className={styles.footL} onClick={props.onOpenSettings}>
          <Icon icon={Settings} size="small" /> 设置
        </span>
      </div>
    </Flexbox>
  );
}
```

> 重命名交互：菜单"重命名"应调用 `onRequestRename(path)` 使该行进入 `editing`。任务 8 的 `SessionItem` 菜单当前把"重命名"接到 `onRename(title)`；在本任务集成时，改为由 `ProjectGroup` 传入的菜单回调触发 `onRequestRename`。实现时统一为：`SessionItem` 增加 `onRequestRename` 回调，菜单"重命名"项调用它，提交走 `onRename(name)`。（见任务 12 步骤 1 的微调）
> 项目别名重命名首版用 `window.prompt` 兜底，二期可换 lobe 内联编辑。

- [ ] **步骤 2：类型检查**

运行：`npx tsc --noEmit`
预期：PASS

- [ ] **步骤 3：Commit**

```bash
git add tauri-agent/src/features/sessions/Sidebar.tsx
git commit -m "feat(sidebar): assemble Sidebar container"
```

---

## 任务 12：`SessionItem` 重命名回调微调 + `App.tsx` 集成

**文件：**
- 修改：`tauri-agent/src/features/sessions/SessionItem.tsx`
- 修改：`tauri-agent/src/features/sessions/useSessionMenu.ts`（菜单回调）
- 修改：`tauri-agent/src/features/sessions/ProjectGroup.tsx`（透传 onRequestRename）
- 修改：`tauri-agent/src/App.tsx`

- [ ] **步骤 1：`SessionItem` 菜单"重命名"改为请求进入编辑**

在 `SessionItemProps` 增加 `onRequestRename: () => void`，并把 `buildSessionMenuItems` 的 `onRename` 接到它：

```tsx
// SessionItemProps 增加：
  onRequestRename: () => void;

// 组件内 items 改为：
  const items = buildSessionMenuItems({
    pinned,
    onPinToggle,
    onRename: onRequestRename, // 点"重命名" → 上层把本行切到 editing
    onDelete,
  });
```

`ProjectGroup` 给 `SessionItem` 传 `onRequestRename={() => p.onRequestRename(s.path)}`。

- [ ] **步骤 2：改 `App.tsx` —— workspace 状态化 + 跨项目切换 + 挂 Sidebar**

将常量 `WORKSPACE` 用法改为来自 `useSessionStore().activeWorkspace`，并在初始化时拉全量会话：

```tsx
// 顶部新增 import
import { Sidebar } from './features/sessions/Sidebar';
import { useSessionStore } from './store';
import { pi } from './lib/pi';

// refreshAllSessions：拉全量会话填充侧边栏
async function refreshAllSessions(): Promise<void> {
  const { setAllSessions, setError } = useSessionStore.getState();
  try {
    setAllSessions(await pi.listAllSessions());
  } catch (err) {
    setError(err instanceof Error ? err.message : String(err));
  }
}
```

`Workspace` 组件内，替换 `<SessionList .../>` 为：

```tsx
<Sidebar
  runningSessionPath={isStreaming ? activeSessionPath : null}
  onToggleSidebar={toggleSidebar}
  onOpenSettings={() => { /* 复用现有设置入口；暂留空或打开设置面板 */ }}
  onNewSession={async (cwd) => {
    await switchProject(cwd);
    await pi.newSession(cwd);
    store.reset();
    await refreshAllSessions();
  }}
  onOpenSession={async (cwd, path) => {
    await switchProject(cwd);
    await pi.switchSession(cwd, path);
    const { messages } = await pi.getMessages(cwd);
    store.loadMessages(messages, { force: true });
    useSessionStore.getState().setActiveSession(path);
  }}
  onDeleteSession={async (cwd, path) => {
    await pi.deleteSession(cwd, path);
    await refreshAllSessions();
  }}
  onSubmitRename={async (cwd, _path, name) => {
    await switchProject(cwd);
    await pi.setSessionName(cwd, name);
    await refreshAllSessions();
  }}
/>
```

其中 `switchProject` 实现"切到目标项目 cwd"（驱动 AgentStoreProvider 重建）：

```tsx
const switchProject = async (cwd: string) => {
  const { activeWorkspace, setActiveWorkspace } = useSessionStore.getState();
  if (activeWorkspace === cwd) return;
  await pi.openWorkspace(cwd);
  setActiveWorkspace(cwd);
};
```

并把 `AgentStoreProvider` 的 workspace 接上状态：

```tsx
// App() 根组件
const activeWorkspace = useSessionStore((s) => s.activeWorkspace);
// ...
<AgentStoreProvider workspace={activeWorkspace}>
  <Workspace />
</AgentStoreProvider>
```

`activeSessionPath` / `isStreaming` 从对应 store 读取（`isStreaming` 来自 `useAgentStore().useStore((s)=>s.isStreaming)`，`activeSessionPath` 来自 `useSessionStore`）。

启动时：`refreshAllSessions()` 与现有 `refreshSessions` 并行调用；`activeWorkspace` 初值 `'.'` 由 `open_workspace('.')` 解析为真实 cwd 后，用解析结果 `setActiveWorkspace`（可在 `openWorkspace` 成功后用 `list_pi_sessions` 首条的 cwd 校正，或新增一个返回 cwd 的命令；首版可直接保留 `'.'` 并在 `useProjectGroups` 里用 `paths` 宽松匹配——**简单起见首版 current 用 '.' 字符串匹配 cwd 失败时，退化为"无当前项目高亮"，不影响功能**）。

- [ ] **步骤 3：类型检查 + 构建**

运行：`npx tsc --noEmit`
预期：PASS

- [ ] **步骤 4：删除旧 `SessionList.tsx` 并清理引用**

```bash
git rm tauri-agent/src/features/sessions/SessionList.tsx
```
确认 `src/features/sessions/index.ts` 不再导出它（改为导出 `Sidebar`）。

运行：`npx tsc --noEmit`
预期：PASS（无对 `SessionList` 的悬空引用）

- [ ] **步骤 5：Commit**

```bash
git add -A tauri-agent/src
git commit -m "feat(sidebar): integrate cross-project Sidebar into App, drop SessionList"
```

---

## 任务 13：联调与全量验证

- [ ] **步骤 1：跑全部前端测试**

运行：`npx vitest run`（工作目录 `tauri-agent`）
预期：全绿（含新增 store/hook/component 测试 + 原有 94 项）

- [ ] **步骤 2：类型检查**

运行：`npx tsc --noEmit`
预期：PASS

- [ ] **步骤 3：Rust 检查**

运行：`cargo check`（工作目录 `tauri-agent/src-tauri`）
预期：PASS

- [ ] **步骤 4：手动冒烟（dev）**

运行：`npm run tauri dev`，验证：
- 侧边栏显示置顶/项目分区，当前项目展开
- 点别的项目会话 → 切换并加载该会话消息
- hover 行出 SquarePen + ⋯；⋯/右键弹菜单；置顶/重命名/删除/在资源管理器打开生效
- 仅当前正在跑的会话显示金色转圈
- 亮/暗主题下侧边栏配色正常（走 token）

- [ ] **步骤 5：最终 Commit（如有手动修复）**

```bash
git add -A tauri-agent
git commit -m "test(sidebar): finalize sidebar redesign integration"
```

---

## 自检（计划 vs 规格覆盖）

| 规格条目 | 对应任务 |
|---------|---------|
| `list_all_sessions` 不过滤 cwd | 任务 1 |
| 前端 `listAllSessions` / `setSessionName` | 任务 2 |
| `sidebarPrefsStore`（置顶/别名/隐藏/展开，persist） | 任务 3 |
| `useSessionStore` 扩展 activeWorkspace/搜索 | 任务 4 |
| `useProjectGroups`（分组/排序/当前优先/过滤） | 任务 5 |
| 项目/会话菜单（Pin/Reveal/Rename/Hide/Delete） | 任务 6 |
| RowActions（SquarePen + ⋯） | 任务 7 |
| SessionItem（无#号、活跃运行态、内联重命名） | 任务 8 |
| ProjectItem/ProjectGroup（无图钉前缀、查看全部 5+N） | 任务 9 |
| 顶部动作 + 搜索 | 任务 10 |
| 三段式装配 + 底部设置/主题 | 任务 11 |
| 跨项目切换（open_workspace+switch，AgentStoreProvider 重建） | 任务 12 |
| 错误处理（list 失败不清空、删除/重命名兜底） | 任务 1/12（store error + refresh 失败保留） |
| 测试策略 | 任务 3/5/6/8 单元 + 任务 13 全量 |

**延后到二期（YAGNI，已在规格标注）：** 在终端打开、后端全文搜索、全量历史状态、拖拽排序。

**已知首版降级点（非占位，明确写出）：**
- 项目别名重命名用 `window.prompt`（二期换内联编辑）
- 当前项目高亮依赖 `activeWorkspace === cwd` 字符串匹配；若 `'.'` 未校正为绝对 cwd，则退化为无高亮，不影响其余功能。建议二期加一个返回绝对 cwd 的命令做精确匹配。
